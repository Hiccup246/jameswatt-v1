import * as React from 'react'

import './project-description.css'

const ProjectDescription = ({ description }) => {
  return <div className="projects__card-description">{description}</div>
}

export default ProjectDescription
